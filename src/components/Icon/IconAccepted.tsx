import React from 'react';
import { Icon } from './Icon';
import image from '../../assets/icons/accepted.svg';

export function IconAccepted(): JSX.Element {
  return <Icon className="opacity-50" src={image} />;
}
